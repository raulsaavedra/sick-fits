schema: http://localhost:3000/api/graphql
generates:
  lib/types/codegen.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      typesPrefix: T
      withHooks: true
